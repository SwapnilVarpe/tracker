import 'package:flutter/material.dart';

class MoneyStats extends StatelessWidget {
  const MoneyStats({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Money stats');
  }
}
